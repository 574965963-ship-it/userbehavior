# 淘宝用户行为数据 · 漏斗分析项目记录（求职作品）

> 数据分析实习求职作品 | 2026-08 完成 | 数据源：阿里天池公开数据集

---

## 一、项目概览

用阿里天池公开的「淘宝用户行为数据集」完成从**导入 → 体检 → 清洗 → 漏斗分析 → 品类复购**的完整流程。

- **数据**：天池 dataset 649，**100,150,807 行**，约 3.5GB，无表头 CSV（逗号分隔）
- **字段（5 列，按顺序）**：
  | 字段 | 含义 | 说明 |
  |---|---|---|
  | user_id | 用户ID | 脱敏 |
  | item_id | 商品ID | 脱敏 |
  | category_id | 类目ID | 脱敏 |
  | behavior_type | 行为类型 | `pv`浏览 / `cart`加购 / `fav`收藏 / `buy`购买 |
  | ts | 时间戳 | Unix 秒级 |
- **时间范围**：官方口径 2017-11-25 ~ 2017-12-03（北京时间）
- **行为链路构成漏斗**：`pv → cart/fav → buy`

---

## 二、环境与导入

**环境**：MySQL 8.0.16 + DataGrip。

**建表**（ID 用 INT UNSIGNED 省空间，列名避开关键字）：

```sql
CREATE DATABASE IF NOT EXISTS taobao CHARACTER SET utf8mb4;
USE taobao;

CREATE TABLE userbehavior (
    user_id       INT UNSIGNED NOT NULL COMMENT '用户ID',
    item_id       INT UNSIGNED NOT NULL COMMENT '商品ID',
    category_id   INT UNSIGNED NOT NULL COMMENT '类目ID',
    behavior_type VARCHAR(10)  NOT NULL COMMENT '行为: pv/cart/fav/buy',
    ts            INT UNSIGNED NOT NULL COMMENT 'Unix秒级时间戳'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

**导入**：

```sql
LOAD DATA LOCAL INFILE 'D:/userbehavior/UserBehavior.csv'
INTO TABLE userbehavior
FIELDS TERMINATED BY ','
LINES TERMINATED BY '\n'
(user_id, item_id, category_id, behavior_type, ts);
```

导入结果 **100,150,807 行**，与官方文档记录完全一致 → 数据没丢。

---

## 三、数据体检（验证）


**① 行为类型分布（应只有 4 种）**
```sql
SELECT behavior_type, COUNT(*) FROM userbehavior GROUP BY behavior_type;
```

**② 时间范围（转北京时间）**
```sql
SET time_zone = '+08:00';
SELECT MIN(FROM_UNIXTIME(ts)), MAX(FROM_UNIXTIME(ts)) FROM userbehavior;
```

**③ 缺失/非法值（应全为 0）**
```sql
SELECT SUM(behavior_type='') empty_behavior, SUM(ts=0) zero_ts,
       SUM(user_id=0) zero_user, SUM(item_id=0) zero_item
FROM userbehavior;
```

**体检发现的异常**：
- `zero_ts = 318`（318 行时间戳为 0）
- 时间范围出现 `1970-01-01 08:00:00`（= ts 为 0）和 `2037-04-09`（= 未来时间戳）

---

## 四、脏数据定位

**① 按天分布（一眼看出异常集中在哪）**
```sql
SET time_zone = '+08:00';
SELECT DATE(FROM_UNIXTIME(ts)) d, COUNT(*) cnt
FROM userbehavior
GROUP BY DATE(FROM_UNIXTIME(ts))
ORDER BY d;
```
结果：核心 9 天（2017-11-25 ~ 12-03）每天 1000 万~1370 万条正常；另散布着 1970 到 2037 之间的小碎数（1~800 条/天）。

**② 抽出坏行并看结构**
```sql
SELECT user_id,item_id,category_id,behavior_type,ts
FROM userbehavior
WHERE ts=0 OR ts>1512316800 LIMIT 20;
```

**结论**：
1. **字段完好，只有 ts 错**——坏行里 user_id/item_id/category_id/behavior_type 都正常 →是时间戳坏了 → 需按时间窗口过滤。
2. **脏数据规模约 5.5 万行**（100,150,807 − 100,095,231 ≈ 55,576），占比 **0.055%**。


---

## 五、清洗方案与 SQL（三步）

**时间边界**：`1511539200` = 2017-11-25 00:00:00，`1512316800` = 2017-12-04 00:00:00。

**① 量化脏数据**
```sql
SELECT COUNT(*) total,
       SUM(ts>=1511539200 AND ts<1512316800) in_range,
       SUM(ts<1511539200)  before_range,
       SUM(ts>=1512316800) after_range
FROM userbehavior;
```

**② 建清洗后的表**
```sql
SET time_zone = '+08:00';
CREATE TABLE userbehavior_clean AS
SELECT user_id, item_id, category_id, behavior_type,
       FROM_UNIXTIME(ts) dt,        -- 标准日期时间（北京）
       DATE(FROM_UNIXTIME(ts)) d    -- 只留日期
FROM userbehavior
WHERE ts >= 1511539200 AND ts < 1512316800;
```

**③ 验证清洗结果**
```sql
SELECT COUNT(*) FROM userbehavior_clean;          -- 应约 100,095,231
SELECT MIN(d), MAX(d) FROM userbehavior_clean;    -- 2017-11-25 ~ 2017-12-03
```

---


## 六、漏斗分析（业务问题：转化漏斗优化）

> 定业务问题：**整体转化漏斗的流失点在哪个环节，怎么优化？** 。

### 口径说明

1. **计数单位用"去重用户-商品对"**：用 `COUNT(DISTINCT user_id, item_id)` ，因为同一用户会多次浏览同一商品（事件会重复），所以去重到(人,商品)对。
2. **漏斗有分支，不是一条直线**：用户可以直接买，也可以加购/收藏后再买，转化率必须分开算。

### 整体漏斗 SQL 与结果

```sql
SELECT behavior_type, COUNT(DISTINCT user_id, item_id) AS uv_item
FROM userbehavior_clean
GROUP BY behavior_type;
```

| 行为 | 去重用户-商品对 |
|---|---|
| 浏览 pv | 71,015,192 |
| 加购 cart | 5,386,584 |
| 收藏 fav | 2,869,977 |
| 购买 buy | 1,926,027 |

- **浏览 → 加购/收藏 ≈ 11.63%**（流失 88%）
- **浏览 → 购买 ≈ 2.71%**

### 口径修正：漏斗是"分支"不是"直线"

如果粗暴算"意向→购买 = 购买/意向 = 1,926,027 / 8,256,561 = 23.33%"，**这是错的**——因为它假设"所有购买都经过了加购/收藏"。但决策路径分析发现：**76% 的购买是直接买，根本没经过加购/收藏**。

正确口径是**分支漏斗**：

```
浏览 7100 万对 (100%)
 ├── 直接购买 146 万对 (2.06%)            ← 看中就买，没加购没收藏
 └── 加购/收藏 826 万对 (11.63%)          ← 高意向
       └── 最终购买 46 万对 (占意向 5.58%)  ← 94.4% 的加购/收藏最终没买
```

- **意向 → 购买（严格口径）= 461,072 / 8,256,561 = 5.58%**
- 注：意向 826 万 = 加购 539 万 + 收藏 287 万，意向这个分母略大，因为既加购又收藏的客户占比仅0.8%。九成多客户没有转化成功的结论不变，趋势不被影响，在这里属宽松口径。

---

## 八、品类分析（流量黑洞）

**子问题**：哪些品类"流量大但转化差"？找出浪费流量的品类。

```sql
SELECT category_id,
       COUNT(DISTINCT CONCAT(user_id, '-', item_id)) AS pv_uv,
       COUNT(DISTINCT CASE WHEN behavior_type IN ('cart','fav')
             THEN CONCAT(user_id, '-', item_id) END) AS intent_uv,
       COUNT(DISTINCT CASE WHEN behavior_type='buy'
             THEN CONCAT(user_id, '-', item_id) END) AS buy_uv
FROM userbehavior_clean
GROUP BY category_id
ORDER BY pv_uv DESC
LIMIT 200;
```

**发现（流量黑洞）**：头部 TOP10 品类吃掉了 **27.7% 的流量，但转化率只有 0.93%**——远低于整体 2.71%。这些品类是"流量黑洞"：大量曝光被浪费，既没带来购买，也没沉淀为加购/收藏。

> **建议**：对黑洞品类做流量再分配。

---

## 九、时段分析（流量与转化的错峰）

**子问题**：一天中哪个时段流量高、哪个时段转化高？

```sql
SELECT HOUR(dt) AS hr,
       SUM(behavior_type='pv') AS pv_cnt,
       SUM(behavior_type='buy') AS buy_cnt,
       ROUND(SUM(behavior_type='buy') / SUM(behavior_type='pv') * 100, 2) AS conv_pct
FROM userbehavior_clean
GROUP BY HOUR(dt)
ORDER BY hr;
```

**发现**：
- **流量峰值在晚上 21~22 点**，但此时**转化率最低**——晚上只逛不买
- **转化率峰值在上午 10~12 点**，但此时流量不是最高。

> **建议**：晚上大流量时段是"种草/安利"窗口，可推"先加购立减"降低下单决策门槛；上午高转化时段加大投放。

---

## 十、决策路径分析（直接买 vs 加购/收藏后买）

**子问题**：用户到底是怎么买的？直接买为主，还是加购/收藏后买为主？

```sql
SELECT
  SUM(has_cart=0 AND has_fav=0) AS direct_buy,
  SUM(has_cart=1 AND has_fav=0) AS cart_then_buy,
  SUM(has_cart=0 AND has_fav=1) AS fav_then_buy,
  SUM(has_cart=1 AND has_fav=1) AS cart_fav_then_buy
FROM (
  SELECT user_id, item_id,
         MAX(behavior_type='cart') AS has_cart,
         MAX(behavior_type='fav')  AS has_fav
  FROM userbehavior_clean
  GROUP BY user_id, item_id
  HAVING MAX(behavior_type='buy') = 1
) t;
```

| 购买路径 | 数量 | 占比 |
|---|---|---|
| 直接购买（没加购没收藏） | 1,464,955 | **76.1%** |
| 加购后购买 | 331,818 | 17.2% |
| 收藏后购买 | 113,893 | 5.9% |
| 收藏+加购后购买 | 15,361 | 0.8% |

（合计 1,926,027，正好等于整体 buy 数 → 口径自洽，验证通过）

**发现**：
1. **76% 的购买是"看中就买"的直接决策**，用户不经过加购/收藏就下单 → 平台应重点优化"浏览→下单"的顺畅度（详情页信息完整、一键下单、支付无摩擦）。
2. **94.4% 的加购/收藏最终没成交**（826 万意向对里 780 万流失）→ 加购/收藏是高意向信号，但没被"召回"。这 780 万个"加了购物车却走了"的用户，是**购物车召回 / 收藏夹推送**最直接的营销对象。

